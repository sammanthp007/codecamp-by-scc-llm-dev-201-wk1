"""W1D3 Bonus 6 - Categorize URLs

Extract URLs from text and categorize them by domain type (.com, .org, .edu).

Example:
    >>> bonus6_categorize_urls("Visit https://example.com and http://test.org")
    {'.com': ['https://example.com'], '.org': ['http://test.org']}
"""

import re


def bonus6_categorize_urls(text):
    """
    Extract URLs from text and categorize them by domain type (.com, .org, .edu).
    
    Args:
        text (str): The input string to search for URLs
        
    Returns:
        dict: A dictionary with domain types as keys and lists of URLs as values
        
    Example:
        >>> bonus6_categorize_urls("Visit https://example.com and http://test.org")
        {'.com': ['https://example.com'], '.org': ['http://test.org']}
    """
    # Your code here
    pass
